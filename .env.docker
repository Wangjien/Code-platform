# Docker部署环境变量配置文件
# 复制此文件为 .env 并根据实际情况修改

# 应用基础配置
COMPOSE_PROJECT_NAME=code-share-platform
FLASK_ENV=production

# 安全密钥（请务必更改）
JWT_SECRET_KEY=your-super-secure-jwt-secret-key-change-this
SECRET_KEY=your-super-secure-app-secret-key-change-this

# 数据库配置
# 使用MySQL（推荐生产环境）
DB_ROOT_PASSWORD=secure-root-password-change-this
DB_PASSWORD=secure-app-password-change-this

# 使用SQLite（开发/小规模）
# DATABASE_URI=sqlite:///instance/bio_code_share.db

# 跨域配置（替换为实际域名）
ALLOWED_ORIGINS=http://localhost:8080,https://yourdomain.com

# Redis配置
REDIS_URL=redis://redis:6379/0

# 应用配置
APP_NAME=代码分享平台
APP_VERSION=1.0.0

# 监控配置（可选）
# SENTRY_DSN=https://your-sentry-dsn.sentry.io

# 邮件配置（可选）
# MAIL_SERVER=smtp.example.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@example.com
# MAIL_PASSWORD=your-email-password
