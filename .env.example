# ========== 生产环境配置模板 ==========
# 复制此文件为 .env 并修改配置

# ===== 安全配置 =====
# JWT 密钥 (必须修改为随机字符串)
JWT_SECRET_KEY=your-super-secret-key-change-this-in-production

# ===== 域名配置 =====
# 您的域名或服务器IP (用于 CORS 配置)
# 示例: https://code.example.com 或 http://123.456.789.0
ALLOWED_ORIGINS=https://your-domain.com

# ===== 数据库配置 =====
# SQLite (默认)
# DATABASE_URI=sqlite:///instance/bio_code_share.db ## 使用相对路径报错，使用下面的绝对路径
DATABASE_URI=sqlite:////app/backend/instance/bio_code_share.db

# MySQL (可选，取消注释使用)
# DATABASE_URI=mysql+pymysql://biocode:password@mysql:3306/bio_code_share
# MYSQL_ROOT_PASSWORD=rootpassword
# MYSQL_USER=biocode
# MYSQL_PASSWORD=biocode123

######## 上面的DATABASE_URI的格式说明 ###########################
# mysql+pymysql://biocode:biocode123@mysql:3306/bio_code_share
#               └──────┘ └─────────┘ └───┘ └──┘ └─────────────┘
#               用户名     密码      主机  端口    数据库名
################################################################

# ===== 默认管理员账户 =====
# 首次启动时自动创建，建议修改默认密码
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123